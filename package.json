{
  "name": "ogma-sync",
  "version": "1.0.0",
  "description": "Cloudflare Workers relay server for Ogma Table multiplayer",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
